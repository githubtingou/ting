<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.java.ting.entity.SysLog">
        <result column="id" property="id"/>
        <result column="url" property="url"/>
        <result column="" property="method"/>
        <result column="class_method" property="classMethod"/>
        <result column="req_data" property="reqData"/>
        <result column="req_IP" property="reqIP"/>
        <result column="result_data" property="resultData"/>

    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
    </sql>

    <select id="sysLogList" resultMap="BaseResultMap">

        SELECT * from sys_log

    </select>
</mapper>